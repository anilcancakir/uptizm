# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains editor launch configs and tasks; keep it local.
.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release
.artisan/

# Local sibling-package path wiring (dev only)
pubspec_overrides.yaml

# Secrets (never commit; .env.example is the template)
/.env

# ac orchestration artifacts (plan + evidence)
.ac/

# Personal Claude Code settings (local sibling-dir wiring, not shared)
.claude/settings.local.json

# Local QA report (not part of the published example)
/REPORT.md

# playwright-cli scratch snapshots from browser debugging sessions.
.playwright-cli/

CLAUDE.local.md
