# CloudPanel vhost for the `app.uptizm.com` static site.
#
# HOW TO APPLY: CloudPanel > Sites > app.uptizm.com > Vhost, replace everything
# with this file, Save.
#
# This is CloudPanel's own generated template with two additions and one removal.
# Every `{{placeholder}}` is left exactly as the panel wrote it:
#
#   1. ADDED: a `location /` SPA fallback. CloudPanel's static template has none,
#      so `/dashboard` and `/monitors/{id}` would 404 even though the Flutter
#      router owns them.
#   2. ADDED: no-store on Flutter's own entry files. They carry no content hash,
#      so a cached copy pins the app at an old build and a deploy appears to do
#      nothing. `flutter_service_worker.js` is the worst of them: a stale service
#      worker keeps serving old assets from its own cache after everything else
#      has updated.
#   3. REMOVED: the trailing `if (-f $request_filename) { break; }`. It is a
#      no-op here (nothing rewrites after it) and `try_files` states the intent.
#
# Serves the compiled Flutter web client from
# /home/uptizm-app/htdocs/app.uptizm.com. No PHP, no Laravel: the client talks to
# https://api.uptizm.com/api/v1 over CORS with a bearer token.
#
# nginx prefers an exact server_name over a wildcard, so this site wins over the
# `*.uptizm.com` entry on the uptizm.com vhost. `app` is also in the reserved slug
# list (config/status_pages.php), so no tenant can register a status page that
# would try to shadow it.

server {
  listen 80;
  listen [::]:80;
  listen 443 quic;
  listen 443 ssl;
  listen [::]:443 quic;
  listen [::]:443 ssl;
  http2 on;
  http3 off;
  {{ssl_certificate_key}}
  {{ssl_certificate}}
  server_name app.uptizm.com;
  {{root}}

  {{nginx_access_log}}
  {{nginx_error_log}}

  if ($scheme != "https") {
    rewrite ^ https://$host$request_uri permanent;
  }

  location ~ /.well-known {
    auth_basic off;
    allow all;
  }

  {{settings}}

  include /etc/nginx/global_settings;

  index index.html;

  # --- Flutter's own files: never cache --------------------------------------
  #
  # Exact-match locations, which nginx prioritises over the regex block below, so
  # these stay uncached even though `.js` and `.json` are long-cached there.
  location = /index.html                { add_header Cache-Control "no-cache, no-store, must-revalidate" always; }
  location = /flutter_bootstrap.js      { add_header Cache-Control "no-cache, no-store, must-revalidate" always; }
  location = /flutter_service_worker.js { add_header Cache-Control "no-cache, no-store, must-revalidate" always; }
  location = /flutter.js                { add_header Cache-Control "no-cache, no-store, must-revalidate" always; }
  location = /main.dart.js              { add_header Cache-Control "no-cache, no-store, must-revalidate" always; }
  location = /version.json              { add_header Cache-Control "no-cache, no-store, must-revalidate" always; }
  location = /manifest.json             { add_header Cache-Control "no-cache, no-store, must-revalidate" always; }

  # `.env` is bundled as a pubspec asset, which the app REQUIRES: without it
  # flutter_dotenv silently defaults BROADCAST_CONNECTION to null on web and
  # realtime never connects. It ships to every browser, so it holds only public
  # values (the API URL and the Reverb app key). Uncached, or a config change
  # would need a hard refresh per visitor.
  location = /assets/.env               { add_header Cache-Control "no-cache, no-store, must-revalidate" always; }

  # --- Everything else: cache hard -------------------------------------------
  location ~* ^.+\.(css|js|jpg|jpeg|gif|png|ico|gz|svg|svgz|ttf|otf|woff|woff2|eot|mp4|ogg|ogv|webm|webp|zip|swf|wasm|map)$ {
    add_header Access-Control-Allow-Origin "*";
    add_header alt-svc 'h3=":443"; ma=86400';
    expires max;
    access_log off;
  }

  # --- SPA fallback ----------------------------------------------------------
  location / {
    try_files $uri $uri/ /index.html;
  }
}
