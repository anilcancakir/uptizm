{{-- Act 3 has no stage of its own: it plays inside the results table from act 2, so
     the rows going amber are the SAME rows the visitor just watched come back green.
     Kept as a file so the act list in hero-stage.blade.php reads as four acts. --}}
